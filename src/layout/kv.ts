// layout/kv.ts — key-value leaf layout (Milestone 2). Skeleton for M0.

import type { Line } from "../render/styledLine.js";

/** Placeholder: layout a kv leaf into styled lines. */
export function layoutKv(_pairs: Record<string, string>, _width: number): Line[] {
  throw new Error("layout/kv not implemented (Milestone 2)");
}
